<% if signed_in? %>

  <%= form_for :session, url: signout_path, method: 'delete' do |f| %>
    Player 1 - <%= link_to current_user.email, current_user %>
    <%= f.submit "Quit?", :class => "button", id: 'signout' %>
  <% end %>


<% else %>

  <%= link_to "Sign In", signin_path %> | <%= link_to "Sign up", signup_path %>

<% end %>
